export const initialState = {
    document: undefined,
    user: undefined
}

export function reducer(state, { payload, type }) {
    switch (type) {
        default: {
            return state
        }
    }
}
